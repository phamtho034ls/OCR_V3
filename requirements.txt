# Requirements cho OCR Pipeline sổ đỏ/sổ hồng
# GPU: CUDA 12.x (RTX series)
# Python: 3.10+

# ─── Core Web Framework ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.7.0

# ─── Image Processing ────────────────────────────────────────────────────────
# PaddleOCR khai báo cả opencv-python và opencv-contrib-python; Albumentations
# cần bản headless. Giữ cả ba wheel cùng đúng một ABI/version để tránh cv2 bị
# ghi đè bởi các binary khác phiên bản.
opencv-python==4.10.0.84
opencv-contrib-python==4.10.0.84
opencv-python-headless==4.10.0.84
Pillow>=10.0.0
numpy>=1.26.0

# ─── PDF Processing ──────────────────────────────────────────────────────────
PyMuPDF>=1.24.0

# ─── OCR Detection (PaddleOCR) ───────────────────────────────────────────────
# Cho Windows + CUDA 12.x, cài theo hướng dẫn:
# pip install paddlepaddle-gpu==2.6.2.post120 -f https://www.paddlepaddle.org.cn/whl/windows/mkl/avx/stable.html
# Backend hiện chạy detector Paddle trên CPU; không cài đồng thời bản GPU.
paddlepaddle==2.6.2
paddleocr==2.8.1
albumentations==1.4.2

# ─── OCR Recognition (VietOCR) ───────────────────────────────────────────────
vietocr>=0.3.8

# ─── Deep Learning (VietOCR backend) ─────────────────────────────────────────
# PyTorch với CUDA 12.1 (tương thích CUDA 12.x):
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchvision>=0.16.0

# ─── Fuzzy Matching (địa danh + label anchor) ────────────────────────────────
rapidfuzz>=3.9.0

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0      # Required by FastAPI TestClient

# ─── Utilities & Export ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
openpyxl>=3.1.0
psutil>=5.9.0
psycopg2-binary>=2.9.0
setuptools<70
